import 'package:flutter/material.dart';

const Color textColor = Colors.white;
const kTextStyleForWelcome = TextStyle(
  fontSize: 20,
  fontWeight: FontWeight.bold,
  color: textColor
);

const kTextStyleAppName = TextStyle(
    fontSize: 10,
    fontWeight: FontWeight.bold,
    color: textColor
);

const kTextStyleForLoginInfo = TextStyle(
    fontSize: 12,
    fontWeight: FontWeight.bold,
    color: textColor
);
const Color buttonColor = Colors.red;

const double sizeBoxHeight = 10.0;